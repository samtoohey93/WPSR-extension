  //Tmeplate literal, so concatinate the values of the `labelIsNormal` class and merge with the output. 
mergeStrings = () => {
  document.addEventListener('DOMContentLoaded', mergerFunction = () => {
    document.getElementsByClassName('labelIsNormal')
    
  })
}

//Event Listener that fires when the button is pressed under the domain boxes 
alertButton = () => { 
    let str = "http://";
    str.replace("http://", "https://")
    window.alert('Output of domains')
  }
  document.addEventListener('DOMContentLoaded', eventList = () => {
    document.getElementById('runevent')
      .addEventListener('click', alertButton);
  });
